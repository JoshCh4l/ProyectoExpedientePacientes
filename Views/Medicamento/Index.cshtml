@model IEnumerable<ProyectoExpedientePacientes.Models.Medicamento>

<h1>Listado de Medicamentos</h1>

<a class="btn btn-primary mb-3" href="@Url.Action("Agregar", "Medicamento")">
    Agregar Medicamento
</a>

<table id="tablaMedicamentos" class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>ID</th>
            <th>Nombre</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var medicamento in Model)
        {
            <tr>
                <td>@medicamento.Id</td>
                <td>@medicamento.Nombre</td>
                <td>
                    <a asp-action="Modificar"
                       asp-route-id="@medicamento.Id"
                       class="btn btn-warning btn-sm">
                        Modificar
                    </a>

                    <a asp-action="Eliminar"
                       asp-route-id="@medicamento.Id"
                       class="btn btn-danger btn-sm">
                        Eliminar
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>

@section Scripts {
    <script>
        $(document).ready(function(){
            $('#tablaMedicamentos').DataTable({
                language: {
                    url: '//cdn.datatables.net/plug-ins/1.13.4/i18n/es-ES.json'
                }
            });
        });
    </script>
}