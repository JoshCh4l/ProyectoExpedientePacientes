@model IEnumerable<ProyectoExpedientePacientes.Models.Especialidad>

<h1>Listado de Especialidades</h1>

<a class="btn btn-primary mb-3" href="@Url.Action("Agregar", "Especialidad")">
    Agregar Especialidad
</a>

<table id="tablaEspecialidades" class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>ID</th>
            <th>Nombre</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var especialidad in Model)
        {
            <tr>
                <td>@especialidad.Id</td>
                <td>@especialidad.Nombre</td>
                <td>
                    <a asp-action="Modificar"
                       asp-route-id="@especialidad.Id"
                       class="btn btn-warning btn-sm">
                        Modificar
                    </a>

                    <a asp-action="Eliminar"
                       asp-route-id="@especialidad.Id"
                       class="btn btn-danger btn-sm">
                        Eliminar
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>

@section Scripts {
    <script>
        $(document).ready(function(){
            $('#tablaEspecialidades').DataTable({
                language: {
                    url: '//cdn.datatables.net/plug-ins/1.13.4/i18n/es-ES.json'
                }
            });
        });
    </script>
}